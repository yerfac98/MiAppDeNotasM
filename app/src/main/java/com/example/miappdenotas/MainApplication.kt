package com.example.miappdenotas

import android.app.Application

class MainApplication : Application() {
    // Variable temporal para guardar el contenido que se exportará
    var exportContent: String? = null
}